# Rà soát và xóa tàn dư Laravel MVC/Blade

- Ngày giờ: `2026-09-02 16:03` (Asia/Ho_Chi_Minh)
- Trạng thái: `Đã thực hiện`
- Người/AI thực hiện: Codex
- Liên quan đến: cấu trúc thư mục backend sau khi chuyển sang API + React

## Mục tiêu

Kiểm tra toàn bộ project và xóa các tệp/thư mục chỉ phục vụ kiến trúc Laravel MVC/Blade cũ, bao gồm các thư mục `resources/css`, `resources/js`, `resources/views` còn hiển thị nhưng không còn được sử dụng.

## Bối cảnh và đánh giá trước khi làm

- Hiện trạng: controller/view/assets MVC đã được đánh dấu xóa, nhưng các thư mục rỗng vẫn còn trên filesystem và xuất hiện trong VS Code.
- Ràng buộc/rủi ro: không xóa Model/Eloquent, API controller, middleware Sanctum, migration, seeder hoặc tài nguyên React đang dùng; không đụng vào dữ liệu MySQL.
- Quyết định và lý do: tìm tham chiếu trước, chỉ xóa mục được xác nhận là MVC cũ hoặc thư mục rỗng; kiểm thử lại toàn bộ sau dọn dẹp.

## Thay đổi hoặc đề xuất

- Tệp/thư mục đã xóa: toàn bộ `resources/`, `public/build`, `routes/web.php`, favicon Laravel cũ, các thư mục controller MVC rỗng `Admin/`, `Auth/`, `Customer/`, `FieldOwner/` và thư mục asset React rỗng.
- Tệp đã sửa: `routes/api.php`, `bootstrap/app.php`, `tests/Feature/ApiEntrypointTest.php`, `README.md`, `Doc/AI_PROJECT_CONTEXT.md`, `Doc/PROJECT_SUMMARY.md` và bản ghi này.
- Hành vi trước/sau: metadata backend chuyển từ `/` sang `/api`; không thay đổi API nghiệp vụ `/api/v1` hay React; cây source không còn Blade/Laravel Vite cũ.
- Điều không thay đổi: nghiệp vụ, API contract, schema, seed và dữ liệu người dùng.

## Kiểm thử / xác minh

- Tìm toàn project xác nhận `0` file `*.blade.php`; `resources/`, `routes/web.php`, `public/build` không còn tồn tại; không còn thư mục source rỗng trong các vùng đã audit.
- `php artisan test`: đạt `13 test`, `32 assertion`.
- `npm run lint`: đạt, không warning. `npm run build`: đạt, 1.994 module.
- Route: giữ nguyên 40 endpoint `/api/v1`; metadata JSON ở `/api`; health ở `/up`.
- PHP lint: 111 file nguồn, 0 lỗi. Composer validate và `git diff --check` đạt.
- `php artisan view:clear`, `route:clear`, `config:clear`: đạt. Pha cache trong `optimize:clear` không chạy vì Laragon/MySQL đang tắt và cache driver dùng database; đây không phải lỗi mã.
- Việc chưa xác minh: API với dữ liệu MySQL thật chưa smoke lại trong lượt này vì dịch vụ MySQL đang tắt.

## Nguồn tham khảo

- Nguồn nội bộ: `Doc/AI_PROJECT_CONTEXT.md`, `Doc/README.md`, `composer.json`, `package.json`, `bootstrap/app.php`, `routes/`, `app/`, `resources/`, `frontend/` và ảnh cây thư mục người dùng cung cấp ngày `2026-09-02`.
- Nguồn ngoài: không sử dụng.

## Việc tiếp theo

- [x] Xác định toàn bộ tàn dư MVC/Blade.
- [x] Xóa mục đã xác nhận an toàn.
- [x] Chạy toàn bộ kiểm thử và cập nhật tài liệu.
